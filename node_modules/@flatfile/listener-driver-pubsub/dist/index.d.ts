import { EventDriver } from '@flatfile/listener';
import PubNub from 'pubnub';

declare class PubSubDriver extends EventDriver {
    private scope;
    events: Map<any, any>;
    pubnub?: PubNub;
    constructor(scope: string);
    start(immediate?: boolean): Promise<unknown>;
    /**
     * Shutdown the pubnub stream
     */
    shutdown(): void;
}

export { PubSubDriver };
